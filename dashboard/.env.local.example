# Copy this to .env.local and fill in values

# FastAPI backend URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Default academy UUID to show on the home page
# Get this from the DB after seeding: SELECT id FROM academies LIMIT 1;
NEXT_PUBLIC_ACADEMY_ID=replace-with-real-uuid
